version: '3'

services:
  splitstreamer:
    build: .
    image: ghcr.io/MatthieuCoder/splitstreamer:${TAG:-latest}
    volumes:
      - ./config.toml:/app/config.toml
      - ./waiting.webm:/app/waiting.webm
    devices:
      - "/dev/snd:/dev/snd"
      - "/dev/dri:/dev/dri"
    environment:
        GST_DEBUG: "1"
